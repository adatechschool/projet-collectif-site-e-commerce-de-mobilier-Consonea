//page d'inscription / login pour les utilisateurs
//on appelle le composant Login qui reste à adapter aux tables créées

import Login from "../components/Login";

function LoginRegistration() {
return (
    <Login />
);
}
export default LoginRegistration;
